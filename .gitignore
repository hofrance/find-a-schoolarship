# Only publish the web site and essentials
# Start by ignoring everything at the repo root
/*

# Keep these files/folders tracked
!.gitignore
!README.md
!LICENSE

# Keep the Laravel app
!/laravel-app/

# Keep the data folder itself, but ignore everything inside by default
!/data/
/data/*
# Re-include only the detections CSV
!/data/detections.csv

# Python/venv and build artifacts (kept ignored even if under laravel-app/ has its own .gitignore)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv*/
.env
.env.*

# Logs and notebooks
logs/
*.log
.ipynb_checkpoints/
*.ipynb

# Editors/OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Caches and temp
.cache/
tmp/
temp/
